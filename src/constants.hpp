#pragma once

#include <QColor>
#include <QDataStream>
#include <QList>
#include <QString>

namespace SA
{

enum class CustomWidgets
{
    ExampleWidget,
    SystemSettings,
    PhasePortrait3D,
    PhasePortrait2D
};

enum class Sections
{
    Project,
    Graphics,
};

} // namespace SA
